#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <cstring>

using namespace std;
int main()
{
   std::cin.tie(NULL);
   std::cout.tie(NULL);
   std::ios::sync_with_stdio(false);
   
   int input;
   int chk;
   cin >> input;

   while(input > 0)
   {
      if(input % 5 == 0)
      {
         chk += input / 5;
   if(input % 5 == 0)
   {
      printf("%d", input / 3);
   }
   else
   {
      
   }
   printf("%d", input);
   return 0;
}

